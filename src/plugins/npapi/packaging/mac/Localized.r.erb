/* <%= $cmake["NoEditWarning"] %>
 */
 
#include <CoreServices/CoreServices.r>

resource 'STR#' (126) { {
    "Yahoo! BrowserPlus Plug-In"
} };

resource 'STR#' (127) { {
	""
} };

resource 'STR#' (128) { {
    <%= $cmake["MacSTRResources"] %>
} };
